name: Push Workflow
on:
  push:
    branches: "main"

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo “Hello World!!”
      - name: Print goodbye
        run: echo “Bye Bye Bye~”